# ===============================================
# SafeSite API - Environment Configuration
# ===============================================

# Database Configuration
# For production, use your Neon/Railway/Supabase connection string
# Example: postgresql://user:pass@host:5432/safesite_db?sslmode=require
DATABASE_URL=postgresql://username:password@localhost:5432/safesite_db

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
# SECURITY: Generate a strong secret using: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_here
JWT_EXPIRES_IN=7d

# CORS Configuration
# Add your frontend URLs (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://yourdomain.com

# Rate Limiting
# 15 minutes window with 100 requests max per IP
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Security
# Higher = more secure but slower (10-12 recommended)
BCRYPT_SALT_ROUNDS=12

# Logging
# Options: error, warn, info, http, verbose, debug, silly
LOG_LEVEL=info

# ===============================================
# Production Deployment Notes:
# ===============================================
# 1. Never commit .env file to Git
# 2. Use platform environment variables (Render, Railway, Fly.io)
# 3. Enable SSL for database connections (sslmode=require)
# 4. Use strong JWT_SECRET (minimum 32 characters)
# 5. Restrict ALLOWED_ORIGINS to your actual frontend domain
# 6. Set NODE_ENV=production for production deployments